{
  "type": "module",
  "devDependencies": {
    "chai": "^4.3.10",
    "coveralls": "^3.1.1",
    "mocha": "^10.2.0",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^15.1.0"
  },
  "scripts": {
    "test": "mocha \"tests/**/*.js\"",
    "coverage": "nyc --reporter=lcov npm test && coveralls < coverage/lcov.info"
  }
}
